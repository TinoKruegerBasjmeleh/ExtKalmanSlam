
# To use:
#
#     pre-commit run -a
#
# Or:
#
#     pre-commit install  # (runs every time you commit in git)
#
# To update this file:
#
#     pre-commit autoupdate
#
# See https://github.com/pre-commit/pre-commit
exclude: ".pgm$|.svg$|external/"
repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-added-large-files
      - id: check-ast
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-symlinks
      - id: check-xml
      - id: check-yaml
        args: ["--allow-multiple-documents"]
      - id: debug-statements
      - id: end-of-file-fixer
      - id: mixed-line-ending
      - id: trailing-whitespace
        exclude_types: [rst]
      - id: fix-byte-order-marker
-   repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.31.1
    hooks:
      - id: check-github-workflows
        args: ["--verbose"]
      - id: check-github-actions
        args: ["--verbose"]
      - id: check-dependabot
        args: ["--verbose"]
-   repo: local
    hooks:
      - id: cmakelint
        name: cmakelint
        description: Check CMake code style using cmakelint.
        language: system
        types: [cmake]
        entry: cmakelint
      - id: cpplint
        name: cpplint
        description: Code style checking using cpplint.
        language: system
        types: [c++]
        entry: cpplint
      - id: xmllint
        name: xmllint
        description: Check XML markup using xmllint.
        language: system
        types: [xml]
        entry: xmllint
        args: ['--noout']
-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.10
    hooks:
        # Run the linter.
      - id: ruff-check
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format
-   repo: local
    hooks:
      - id: clang-format
        name: clang-format
        description: Format files with ClangFormat.
        entry: clang-format
        language: system
        files: \.(c|cpp|h|hpp)$
        args: ['-fallback-style=none', '-i']

      # - id: ament_clang_tidy
      #   name: ament_clang_tidy
      #   description: Checks and fixes C++ code for style violations, bugs, and performance issues
      #   language: system
      #   types: [c++]
      #   entry: ./scripts/hooks/docker_exec.sh ament_clang_tidy
